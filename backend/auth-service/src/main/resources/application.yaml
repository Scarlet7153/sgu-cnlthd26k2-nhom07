server:
  port: ${PORT:8085}

spring:
  application:
    name: ${SPRING_APPLICATION_NAME:auth-service}
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://voduytoan:Voduytoan7153@pcshop.91getcf.mongodb.net/user_db?retryWrites=true&w=majority&appName=PCShop}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${EMAIL_USERNAME:pcshop7153@gmail.com}
    password: ${EMAIL_PASSWORD:tvpwqiovicyfkbfq}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS_ENABLE:true}
            required: ${MAIL_SMTP_STARTTLS_REQUIRED:true}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: ${EUREKA_PREFER_IP_ADDRESS:true}

jwt:
  secret: ${JWT_SECRET:pcshop-jwt-secret-key-2026-must-be-at-least-256-bits-long-for-hs256}
  access-token-expiration: ${ACCESS_TOKEN_EXPIRATION:3600000}
  refresh-token-expiration: ${REFRESH_TOKEN_EXPIRATION:604800000}

management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info}
  