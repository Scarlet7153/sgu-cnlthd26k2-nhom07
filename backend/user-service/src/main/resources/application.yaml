server:
  port: 8081

spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: mongodb+srv://voduytoan:Voduytoan7153@pcshop.91getcf.mongodb.net/user_db?retryWrites=true&w=majority&appName=PCShop

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# JWT Configuration
jwt:
  secret: pcshop-jwt-secret-key-2026-must-be-at-least-256-bits-long-for-hs256
  access-token-expiration: 3600000    # 1 hour in ms
  refresh-token-expiration: 604800000 # 7 days in ms

management:
  endpoints:
    web:
      exposure:
        include: health,info
