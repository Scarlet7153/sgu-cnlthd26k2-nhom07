server:
  port: 8082

spring:
  application:
    name: product-service
  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb+srv://voduytoan:Voduytoan7153@pcshop.91getcf.mongodb.net/product_db?retryWrites=true&w=majority&appName=PCShop

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
