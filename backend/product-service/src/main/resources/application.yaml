server:
  port: ${PORT:8082}

spring:
  application:
    name: ${SPRING_APPLICATION_NAME:product-service}
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://voduytoan:Voduytoan7153@pcshop.91getcf.mongodb.net/product_db?retryWrites=true&w=majority&appName=PCShop}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: ${EUREKA_PREFER_IP_ADDRESS:true}

management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info}
