server:
  port: 8084

spring:
  application:
    name: payment-service
  data:
    mongodb:
      uri: mongodb+srv://voduytoan:Voduytoan7153@pcshop.91getcf.mongodb.net/payment_db?retryWrites=true&w=majority&appName=PCShop

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# MoMo Sandbox Configuration
momo:
  partner-code: MOMO
  access-key: F8BBA842ECF85
  secret-key: K951B6PE1waDMi640xX08PD3vg6EkVlz
  api-endpoint: https://test-payment.momo.vn/v2/gateway/api/create
  redirect-url: ${FRONTEND_URL:http://localhost:5173}/payment/result
  ipn-url: ${BACKEND_URL:http://localhost:8080}/api/payments/callback/momo
  request-type: payWithMethod

management:
  endpoints:
    web:
      exposure:
        include: health,info
